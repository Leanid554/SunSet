// src/components/LockIcon.jsx

import React from "react";
import "./LockIcon.scss";

function LockIcon() {
  return (
    <div className="lock-icon">
      {/* Простой замок в виде элемента */}
      <i className="fas fa-lock"></i>
    </div>
  );
}

export default LockIcon;
